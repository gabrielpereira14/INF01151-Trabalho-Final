int main() {
    // Cria o socket

    // Faz bind no port de conecção da interface

    while (1) {
        // Espera uma requisição de conecção

        // Verifica se a conecção é válida e responde para o cliente

        // Se válida, cria os sockets de receber e enviar arquivos

        // Guarda os dados da conecção e lança as threads
    }
    
    return 0;
}